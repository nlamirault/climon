------------
-- climon --
------------

* Required softwares *

  You will need :

  - ASDF : http://www.cliki.net/asdf
  - PAL : http://common-lisp.net/project/pal

* ASDF *

  In /usr/share/common-lisp/systems, create a symbolic link
  to the system definition file (climon.asd)
  or
  add the ernestine directory to *central-directory* of ASDF :
  (push "/directory/climon/" asdf:*central-registry*)

* Load *
 
  In the Lisp system :
  (asdf:operate 'asdf:load-op 'climon)

* Launch game *

 CL-USER> (climon:climon)



Local variables:
mode: outline
paragraph-separate: "[  ^L]*$"
end:
